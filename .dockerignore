# Build output
target/
build/
out/

# IDE & OS
.idea/
.vscode/
*.iml
.DS_Store

# Git
.git/
.gitignore
.gitattributes

# Local env / secrets — never bake these into the image
.env
.env.*
!.env.example

# Tests artefacts
**/test-output/
**/surefire-reports/

# Logs, dumps
*.log
*.tmp
hs_err_pid*

# Docker meta (no need to ship into the build context)
Dockerfile
.dockerignore
docker-compose*.yml

# Local uploads / persistence
var/
